device:
  id: "pc-simulado-obra-demo-01"
  obra: "OBRA DEMO"

storage:
  raw_db_path: "data/segitec_asistencia.db"
  workers_cache_path: "data/workers_demo.json"
  raw_retention_days: 90

google:
  enabled: false
  api_url: "http://127.0.0.1:8000/exec"
  timeout_seconds: 10

rfid:
  mode: "simulated"
